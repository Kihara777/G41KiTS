# api Service — 对应 nginx upstream "api { server ra:5800; }"
apiVersion: v1
kind: Service
metadata:
  name: ra
  namespace: g41
  labels:
    app.kubernetes.io/part-of: g41
    app.kubernetes.io/name: api
spec:
  selector:
    app: api
  ports:
    - { name: http, port: 5800, targetPort: 5800 }
