# hako Service — 对应 nginx upstream "fb { server hako:80; }"
apiVersion: v1
kind: Service
metadata:
  name: hako
  namespace: g41
  labels:
    app.kubernetes.io/part-of: g41
    app.kubernetes.io/name: hako
spec:
  selector:
    app: hako
  ports:
    - { name: http, port: 80, targetPort: 80 }
