# bt Service — 对应 nginx upstream "tr { server bt:9091; }"
apiVersion: v1
kind: Service
metadata:
  name: bt
  namespace: g41
  labels:
    app.kubernetes.io/part-of: g41
    app.kubernetes.io/name: bt
spec:
  selector:
    app: bt
  ports:
    - { name: rpc, port: 9091, targetPort: 9091 }
